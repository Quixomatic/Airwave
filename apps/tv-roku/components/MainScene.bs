' MainScene code-behind.
' Stage 1: prove the BrighterScript -> SceneGraph -> roku-deploy loop renders on a real Roku.
' Next: replace this with the onboarding / server-setup screen (port of tv-web/tv-native).
sub init()
    print "[Airwave] MainScene init"
    m.brand = m.top.findNode("brand")
    m.tagline = m.top.findNode("tagline")
end sub
