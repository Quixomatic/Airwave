# Build output
out/
*.zip

# On-demand device screenshots (scripts/grab-screenshot.ts) — local only
screenshots/

# Store-submission artifacts (.rasp scripts, listing copy, reviewer/exemption notes) — local only
cert/

# Secrets: your Roku dev-mode IP + password live here (copy from rokudeploy.example.json)
rokudeploy.json

# Deps
node_modules/
