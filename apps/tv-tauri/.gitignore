# Frontend build
dist/

# Rust / Tauri build output
src-tauri/target/
src-tauri/gen/

# Vendored libmpv binaries — big + reproducible from our CI (tools/libmpv + the
# libmpv-latest release). Fetched per os-arch in CI (the tarball is self-contained:
# bin/ runtime DLLs + lib/mpv.lib import lib). NOT committed. Headers (include/) tracked.
src-tauri/resources/lib/
src-tauri/vendor/libmpv/windows-*/
src-tauri/vendor/libmpv/macos-*/
src-tauri/vendor/libmpv/linux-*/
routeTree.gen.ts
