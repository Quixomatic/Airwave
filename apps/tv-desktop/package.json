{
  "name": "tv-desktop",
  "private": true,
  "version": "0.11.41",
  "description": "Airwave native desktop client (Native SDK). Editor + versioning surface only: stock TypeScript tooling resolves @native-sdk/core from here; the native CLI builds with node_modules absent. Requires the global `native` CLI (npm i -g @native-sdk/cli) + Zig 0.16.",
  "scripts": {
    "dev": "native dev",
    "build": "native build",
    "check": "native check",
    "package": "native package --target windows"
  },
  "dependencies": {
    "@native-sdk/core": "0.9.5"
  },
  "devDependencies": {
    "@native-sdk/cli": "0.9.5"
  }
}
